/** Chat Pane — LLM 대화 입력/응답. P1.2 에서 SSE 연동. */
export function ChatPane() {
  return (
    <div className="border rounded-lg bg-white dark:bg-gray-900 p-4 flex flex-col">
      <h2 className="text-sm font-semibold mb-2">Chat</h2>
      <div className="flex-1 text-xs text-gray-400">
        프롬프트 UI는 P1.2 에서 구현됩니다.
      </div>
    </div>
  );
}
