/** Code Pane — Monaco Editor. P1.2 에서 @monaco-editor/react 통합. */
export function CodePane() {
  return (
    <div className="border rounded-lg bg-white dark:bg-gray-900 p-4 flex flex-col">
      <h2 className="text-sm font-semibold mb-2">Code</h2>
      <div className="flex-1 text-xs text-gray-400">
        Monaco Editor는 P1.2 에서 통합됩니다.
      </div>
    </div>
  );
}
